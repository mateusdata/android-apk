import { StyleSheet } from "react-native";

const styleEngloba = StyleSheet.create({
  view: {
      flex: 0.98,
      borderWidth: 10,
      borderColor: "blue",
      alignItems: "center",
      justifyContent: "flex-start",
      },
})
export default styleEngloba;